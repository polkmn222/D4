Test-specific documentation lives here.

- `implementation_*` captures what changed to support testing work.
- `walkthrough_*` captures the validation narrative.
- `task_*` captures the original test-focused task briefs.

These files are archived references and are kept separate from the main project-wide documentation folders.

Current testing commands and active validation policy now live under `development/docs/testing/`.
Use `development/test/docs/` only for archived phase-oriented notes, not for the latest run instructions.
