Phase 304 implementation

- Restored the AI Agent header debug toggle and kept it hidden only in minimized state.
- Increased the default AI Agent window height slightly while preserving 90% base zoom and 95% maximized zoom.
- Changed AI Agent Send Message composer to lazy-load AI recommendations only when the `AI Recommend` button is clicked.
- Added `Save Recipients` and `Clear All` actions to the AI Agent Send Message composer.
- Renamed the web Send Message secondary action from `Preview Message` to `Save Recipients`.

