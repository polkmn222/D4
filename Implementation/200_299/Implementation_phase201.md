# Phase 201 Implementation Plan

## Planned Changes

1. Replace the AI Agent workspace `/new-modal` submit path so it no longer uses `enhanceModalForms()`.
2. Reuse AI-specific redirect handling for both inline chat forms and workspace forms.
3. Add focused tests guarding the new workspace form wiring contract.
