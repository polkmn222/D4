# Phase 228 Implementation

- Added pytest marker split for `unit` and `integration`.
- Added PostgreSQL integration test fixtures driven by `TEST_DATABASE_URL`.
- Reclassified identified AI-agent DB-backed tests from the unit path to the integration path.
- Updated testing docs to codify PostgreSQL-only DB-backed testing and the unit/integration split.
- Backup reference: `backups/200_299/phase228/`
