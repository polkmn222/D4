## Phase 337 Task

- Scope: fix MessageTemplate detail image rendering when an MMS template already has a saved image but the detail tab shows a broken preview.
- In scope:
  - web template detail image resolution only
- Out of scope:
  - create/edit upload flow
  - AI Agent UI
