# Phase 330 Task Record

## Scope

- Fix the web `message_templates` detail inline image edit so selecting a replacement image shows a visible preview before save.

## Constraints

- Stay within the single requested issue only.
- Read task-relevant docs and project-local `.codex` guidance first.
- Do not use `Implementation/`, `Walkthrough/`, or `backups/` as development reference.
- Unit tests are mandatory.
- Manual testing is forbidden.
- SQLite is forbidden.

## Approved Execution

- User approved proceeding after narrowing docs and `.codex` review to project-helpful files.
- User requested one issue at a time with accuracy prioritized over speed.

## Success Criteria

- The inline image edit area on the web message-template detail view shows a preview card for the current or newly selected image.
- Staged image replacement and staged image removal continue to work with the shared save footer flow.
- Focused unit tests pass.
