## Phase 328 Task

- Add a collapsible saved-recipients table to Send Message so staged recipients are easier to review.
- Replace the Send Message template picker with a searchable lookup-style selector while preserving template apply/save behavior.
- Fix AI Agent results-table search so it filters by visible columns instead of hidden row JSON values.
- Fix AI Agent inline web-form handling so shared modal scripts load for template type visibility and non-phase1 edit forms can save correctly.
- Add unit tests for the updated Send Message and AI Agent behavior.
