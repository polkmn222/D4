## Phase 327 Task

- Make Send Message template selection searchable while preserving the existing select/picklist behavior.
- Remove redundant template preview card from Send Message because the mobile preview already covers the preview use case.
- Fix the AI Agent local results-table search box so users can type continuously without losing focus after the first character.
- Add unit tests for the updated Send Message and AI Agent search behavior.
