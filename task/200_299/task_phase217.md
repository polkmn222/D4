# Phase 217 Task

## Goal

Make the embedded web lead create/edit screen readable inside `Ops Pilot` without moving to a modal flow.

## Scope

- Expand the standalone agent panel layout.
- Give the lead workspace more usable height and width.
- Auto-resize the embedded iframe to the loaded web form document height.
- Update focused unit tests and docs for the new frontend contract.

## Constraints

- Keep the lead create/edit runtime sourced from the real web lead routes.
- Do not switch to modal behavior.
- Validate with unit tests only.
