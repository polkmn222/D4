# Phase 227 Task

- Scope: Phase 2 chat-native `OPEN_FORM` implementation.
- Approved objects: `lead`, `contact`, `opportunity`.
- Approved opportunity fields only:
  `name`, `amount`, `stage`, `status`, `probability`, `temperature`.
- Out of scope:
  opportunity lookup fields,
  slot-filling,
  modal-style UI reuse,
  manual testing.
- Constraints: unit tests only, no expansion beyond the approved Phase 2 UX rule.
