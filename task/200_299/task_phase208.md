# Phase 208 Task

## Goal

Remove AI Agent automatic chat scrolling so form/workspace visibility is not overridden by message auto-scroll.

## Scope

- Remove `appendChatMessage()` auto-scroll behavior.
- Add unit coverage to keep auto-scroll out of the chat-message renderer.
