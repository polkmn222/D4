# Phase 204 Task

## Goal

Fix AI Agent lead table rendering so the `Name` column shows the lead display name instead of `-`.

## Scope

- Update AI Agent lead result-table field mapping to prefer `display_name`.
- Add unit coverage for the lead name rendering contract.
