## Phase 190 Task

- Update canonical markdown documentation so the current codebase, provider behavior, and unit-test commands are accurate.
- Refresh test instructions to prefer `PYTHONPATH=development` and SQLite-based local runs for network-safe unit verification.
- Verify whether MMS can be sent through the configured provider path without manual testing.
- Record any environment blocker that prevents real carrier MMS verification.
