[pytest]
cache_dir = development/.pytest_cache
markers =
    unit: Pure logic tests that use mocks and do not rely on real DB persistence.
    integration: PostgreSQL-backed tests that exercise ORM, CRUD persistence, queries, or schema behavior.
